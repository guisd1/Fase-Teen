import type { NewProductRow } from "../schema";

/*
  Produtos iniciais da Ciranda Cirandinha, gravados no banco por
  `npm run db:seed`. Deixe vazio para começar sem produtos.
*/
const seed: NewProductRow[] = [];

export default seed;
