import type { NewProductRow } from "../schema";

/*
  Produtos iniciais da Fase Teen, gravados no banco por `npm run db:seed`.
  Vazio: os produtos são cadastrados pelo painel de administrador.
*/
const seed: NewProductRow[] = [];

export default seed;
